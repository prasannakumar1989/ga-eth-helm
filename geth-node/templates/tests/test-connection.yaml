apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "geth-node.fullname" . }}-test-connection"
  labels:
    {{- include "geth-node.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "geth-node.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
